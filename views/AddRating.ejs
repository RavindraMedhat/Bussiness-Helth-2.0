<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Performance Ratings</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            margin-top: 20px;
            color: #d9534f; /* Red color */
        }

        form {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            margin-top: 20px;
            color: #d9534f; /* Red color */
        }

        div {
            margin-bottom: 20px;
        }

        label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
            color: #d9534f; /* Red color */
        }

        select,
        input[type="text"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #d9534f;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
    </style>
</head>

<body>
    <h1>Performance Ratings</h1>
    <form action="./submit-ratings" method="POST">

        <!-- <div>
            <h2> <label for="company-name">Company Name:</label></h2>
            <input type="text" name="companyName" id="company-name" required>
        </div> -->
        <% let currentArea='' ; %>
            <% for (let i=0; i < indicators.length; i++) { %>
                <% if (indicators[i].area !==currentArea) { %>
                    <% currentArea=indicators[i].area; %>
                        <h2>
                            <%= currentArea %>
                        </h2>
                        <% } %>
                            <div>
                                <h3>
                                    <%= indicators[i]["performanceIndicator"] %>
                                </h3>
                                <label for="<%= 'rating-' + i %>">Rate (0-10): </label>
                                <select name="<%= 'rating-' + i %>" id="<%= 'rating-' + i %>" required>
                                    <% for (let j=0; j <=10; j++) { %>
                                        <option value="<%= j %>">
                                            <%= j %>
                                        </option>
                                        <% } %>
                                </select>
                            </div>
                            <% } %>
                                <button type="submit">Submit Ratings</button>
    </form>
</body>

</html>
